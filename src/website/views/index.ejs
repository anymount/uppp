<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discord Auth System</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: #000000;
            min-height: 100vh;
            display: flex;
            margin: 20px;
            padding: 0;
            align-items: center;
            justify-content: center;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
        }

        .login-container {
            background-color: #000000;
            border: 1px solid #333;
            border-radius: 16px;
            width: 100%;
            max-width: 600px;
            padding: 60px;
            text-align: center; 
        }

        .logo-section {
            margin-bottom: 24px;
        }

        .logo-section h1 {
            color: #fff;
            font-size: 24px;
            font-weight: 600;
        }

        .logo-section span {
            color: #5865F2;
        }

        .logo-section p {
            color: #666;
            font-size: 14px;
            margin-top: 8px;
        }

        .discord-login-btn {
            width: 100%;
            background: #5865F2;
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 14px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .discord-login-btn:hover {
            background: #4752C4;
            transform: translateY(-2px);
        }

        .discord-login-btn i {
            font-size: 18px;
        }

        .success-message {
            color: #2ecc71;
            font-size: 18px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="login-container">
            <div class="logo-section">
                <h1>Auth<span>System</span></h1>
                <p>Sistema de Autenticação Discord</p>
            </div>

            <% if (!user) { %>
                <a href="/auth/discord" class="discord-login-btn">
                    <i class="fab fa-discord"></i>
                    Entrar com Discord
                </a>
            <% } else if (user.id === '928069145302556693') { %>
                <div class="user-actions" style="display: flex; flex-direction: column; gap: 10px;">
                    <a href="/dashboard" class="discord-login-btn" style="background: #2ecc71;">
                        <i class="fas fa-chart-line"></i>
                        Acessar Dashboard
                    </a>
                    <a href="/auth/logout" class="discord-login-btn" style="background: #e74c3c;">
                        <i class="fas fa-sign-out-alt"></i>
                        Sair
                    </a>
                </div>
            <% } else { %>
                <div class="success-message">
                    Usuário registrado
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>